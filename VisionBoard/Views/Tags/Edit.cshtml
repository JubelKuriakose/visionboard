@model VisionBoard.Models.Tag

@{
    ViewData["Title"] = "Edit";
    Layout = null;
}

<div class="row">
    <div class="col-md-12 p-3">
        <form asp-controller="Tags" asp-action="Edit" asp-route-id="@Model.Id" onsubmit="return AddorEditTag(this);">
            <input type="hidden" name="source" id="source" value="@ViewBag.Source" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="row">
                <div class="col-md-9">
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="col-md-3">
                    <div class="form-group">
                        <label asp-for="Colour" class="control-label"></label>                        
                        @Html.EditorFor(model => model.Colour, new { htmlAttributes = new { @class = "form-control p-0 w-2", @Value = @Model.Colour, required = "required", type = "color" } })
                        <span asp-validation-for="Colour" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
